"use strict"; //Sistema obliga que la semántica a respetar el lenguaje

let total = 0, count = 1;
while (count <= 5) {
    console.log(total + " " + count)
    total += count;
    count++;
}
//console.log(total);
